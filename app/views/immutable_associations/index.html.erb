<h1>Listing immutable_associations</h1>

<table>
  <tr>
    <th>AssociationId</th>
    <th>ObjectId</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @immutable_associations.each do |immutable_association| %>
  <tr>
    <td><%= immutable_association.associationId %></td>
    <td><%= immutable_association.objectId %></td>
    <td><%= link_to 'Show', immutable_association %></td>
    <td><%= link_to 'Edit', edit_immutable_association_path(immutable_association) %></td>
    <td><%= link_to 'Destroy', immutable_association, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Immutable association', new_immutable_association_path %>
